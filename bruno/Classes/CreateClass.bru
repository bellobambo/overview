meta {
  name: Create Class
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/classes
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  "name": "History 201",
  "description": "World history overview"
}
